# tgbot
A multi-threaded Telegram bot daemon written in C11.  Supports long-poll and webhook modes, per-user message queuing with ring buffers, circular file logging, and systemd integration with CLI management commands.
